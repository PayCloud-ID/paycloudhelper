# S3MinIO gRPC SDK

This package is the service-scoped gRPC entrypoint for S3MinIO integrations.

Runtime implementation ownership is fully under `sdk/services/s3minio/grpc`.
Consumers should import this package directly for S3MinIO gRPC adapter behavior.
